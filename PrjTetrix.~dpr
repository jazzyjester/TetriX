program PrjTetrix;

uses
  Forms,
  UMain in 'UMain.pas' {FormMain},
  UBrick in 'UBrick.pas',
  TetrixConst in 'TetrixConst.pas',
  UShape in 'UShape.pas',
  UBoard in 'UBoard.pas',
  UMsg in 'UMsg.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TFormMain, FormMain);
  // Application.CreateForm(TFormMessage, FormMessage);
  Application.Run;
end.
